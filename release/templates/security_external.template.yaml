schema_version: 1
gate: security_external
candidate_id: sha256:REPLACE_FROM_CANDIDATE_JSON
reviewed_commit: REPLACE_WITH_40_CHARACTER_GIT_SHA
reviewed_at: "YYYY-MM-DDTHH:MM:SS+00:00"
outcome: pending
reviewer:
  identifier: REPLACE_WITH_NAME_OR_PUBLIC_HANDLE
  relationship: independent
  expertise: application, LLM, and software-supply-chain security
scope:
  - authentication, network binding, CORS, paths, and SQL boundaries
  - provider and search data boundaries plus prompt-injection containment
  - generated application sandbox, CSP, escaping, and export
  - dependencies, build provenance, SBOM, and release scripts
evidence:
  - path: release/evidence/reports/security-external-report.md
    sha256: REPLACE_WITH_64_CHARACTER_SHA256_AFTER_REPORT_IS_FINAL
artifacts:
  wheel: REPLACE_WITH_64_CHARACTER_SHA256
  sdist: REPLACE_WITH_64_CHARACTER_SHA256
  sbom: REPLACE_WITH_64_CHARACTER_SHA256
details:
  surfaces_reviewed:
    - foundry_pipeline
    - local_http_api
    - generated_app
    - supply_chain
  unresolved_critical: -1
  unresolved_high: -1
attestation: REPLACE_WITH_A_SUBSTANTIVE_HUMAN_ATTESTATION_OF_THE_REVIEW_AND_RESULT
